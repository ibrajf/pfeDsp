import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    primary: "#FFFAF0",      // Replace with your primary color
    secondary: "#00FF00",    // Replace with your secondary color
  },
});

export default theme;
